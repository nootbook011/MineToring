# Class: BaseBedrockBot наследует [BedrockPlugins](./BedrockPlugins.md)

Ядро фреймворка MineToring, отвечающее за инициализацию клиента, управление сессиями, версиями протокола и жизненным циклом бота. Класс обеспечивает полный цикл подключения к серверу Minecraft Bedrock, включая обработку статусов, синхронизацию версий и управление плагинами.

## Содержание
- [Статусы бота](#статусы-бота)
- [Свойства](#свойства)
  - [options](#options)
  - [username](#username)
  - [status](#status)
  - [version](#version)
  - [client](#client)
  - [session](#session)
- [Динамические Свойства](#динамические-свойства)
  - [packets](#packets)
- [Методы](#методы)
  - [async init(options, plugins? = [])](#async-initoptions-plugins--)
  - [async connect()](#async-connect)
  - [disconnect()](#disconnect)
  - [async ping()](#async-ping)
  - [log(type, message, logLevel? = -1)](#logtype-message-loglevel---1)
- [Методы ожидания](#методы-ожидания)
---

## Статусы бота
Доступны через статический геттер `BaseBedrockBot.statusList` и через импорт `BOTSTATES` из `extraConstants`.

| Константа | Значение | Описание |
| :--- | :--- | :--- |
| `NotInitialized` | `0` | Бот создан, но метод `init()` еще не был вызван. |
| `Disconnected` | `1` | Бот инициализирован и готов к подключению, либо отключен от сервера. |
| `Connecting` | `2` | Процесс установки соединения с сервером и загрузка первоначальных данных сервера. |
| `Spawned` | `3` | Бот успешно заспавнился в мире и готов к взаимодействию. |

---

## Свойства

### `options`
**Тип**: [`BotOptionsManager`](./BotOptionsManager.md)

Объект конфигурации бота, содержащий настройки сервера, клиента и сети. Инициализируется в методе `init()`.

* **set**: Если это экземпляр `BotOptionsManager`, то устанавливает значение, если это объект, то пытается создать экземпляр `BotOptionsManager` и установить его.

### `username`
**Тип**: `string`

Текущий никнейм бота на сервере.

### `status`
**Тип**: `number`

Возвращает текущий числовой статус бота (0-3). Используйте `BaseBedrockBot.statusList` для получения имен статусов.

### `version`
**Тип**: `string`

Текущая версия Minecraft Bedrock, используемая ботом (например, `'1.21.50'`). Синхронизируется при инициализации путем пинга сервера или используется значение из конфигурации.

### `client`
**Тип**: `CustomPClient`

Низкоуровневый клиент бота, не рекомендуется изменять в нем какие-либо значения без знания о том, за что они отвечают.

### `session`
**Тип**: `object`

Возвращает копию текущих данных сессии (UUID, XUID, PlayFab ID). Данные из этой сессии сохраняются между переподключениями.
[Документация по сессиям.](../ClientSessions.md)ы

## Динамические Свойства

### `packets`
**Тип**: `BotPacketController`

Дает доступ к низкоуровневому контроллеру пакетов. Может слушать и отправлять пакеты на сервер.

* **Добавляет**: `BotPacketController`

---

## Методы

### `async init(options, plugins? = [])`
Основной метод для подготовки бота. Выполняет синхронизацию версии, инициализацию внутренних компонентов и загрузку плагинов.

**Параметры**:
- `options` (`BotOptionsManager|Object`): Конфигурация бота. Если передан объект, будет преобразован в `BotOptionsManager`.
- `plugins` (`Array`): Массив кастомных плагинов бота.

### `async connect()`
Запускает процесс подключения к серверу.

**Выбрасывает**: `RakTimeout` если сервер не отвечает на пинг, только при включённом параметре `pingBeforeConnect` в настройках бота.

### `disconnect()`
Корректно разрывает соединение с сервером.

### `async ping()`
Пингует сервер для получения информации о нем или проверки доступности.

### `log(type, message, logLevel? = -1)`
Логирует сообщение в консоль и файл через плагин `Logger` класса.

**Параметры**:
- `type` (`string`): Категория лога.
  - 0: debug.
  - 1: info.
  - 2: warn.
  - 3: error.
- `message` (`string`): Текст сообщения.
- `logLevel` (`number`): Уровень логирования.
---

## Методы ожидания
Полезны для синхронизации логики при запуске бота.

| Метод | Описание |
| :--- | :--- |
| `async waitUntilInit()` | Разрешается, когда `init()` завершен и статус >= `Disconnected`. |
| `async waitUntilConnect()` | Разрешается, когда статус >= `Connecting`. Отклоняется при внезапном отключении. |
| `async waitUntilSpawn()` | Разрешается, когда статус === `Spawned`. Отклоняется при внезапном отключении. |
| `async waitUntilDisconnect()` | Разрешается, когда статус <= `Disconnected`. |

---